# OS / editor
.DS_Store
.idea/
.vscode/

# Backend env (see backend/.gitignore for the rest)
backend/.env

# Root compose env (host-port overrides etc.) — machine-specific
/.env

# Frontend
frontend/node_modules/
frontend/dist/
